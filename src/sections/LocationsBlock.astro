---
import { Image } from "astro:assets";

import portlandMap from "../images/home/portland-map.webp";
import seattleMap from "../images/home/seattle-map.webp";
---

<section class="flex flex-col gap-4 p-4 md:p-8 lg:flex-row" id="locations">
  <div class="flex basis-2/3 flex-col gap-4">
    <h2 class="text-xl font-bold lowercase sm:text-2xl bg-white w-fit">where we're at</h2>
    <p class="sm:text-xl bg-white w-fit">
      Are you looking to connect with some bgirls in your city?
    </p>
    <p class="sm:text-xl bg-white w-fit">
      Even if we don't have a CYPHERQueenz crew established in your city yet,
      we'll still do our best to make some introductions for you.
    </p>
    <p class="sm:text-xl">
      <a href="/about" class="flex flex-row gap-2 text-lg md:text-xl">
        <span class="border-b-4 border-[#facc15]">Connect with Us</span>
        →
      </a>
    </p>
  </div>
  <div class="flex basis-1/3 items-start justify-around gap-2">
    <div class="relative bottom-0 w-max border-2 border-[#800000] sm:w-48">
      <Image
        src={seattleMap}
        alt="Seattle Map"
        class="border-b-2 border-[#800000]"
      />
      <p class="bg-white p-3 text-lg">Seattle, WA</p>
    </div>
    <div class="relative bottom-0 w-max border-2 border-[#800000] sm:w-48">
      <Image
        src={portlandMap}
        alt="Portland Map"
        class="border-b-2 border-[#800000]"
      />
      <p class="border-[#800000] bg-white p-3 text-lg">Portland, OR</p>
    </div>
  </div>
</section>

</style>
