---
---

<header class="w-full bg-gray-900">
  <nav class="m-auto flex max-w-4xl flex-wrap items-center justify-between p-6">
    <div class="mr-6 flex flex-shrink-0 items-center text-white">
      <a href="#" class="text-xl font-extrabold tracking-tight">CypherQueenz</a>
    </div>
    <div class="block sm:hidden">
      <button id="hamburger" class="flex items-center rounded border border-teal-400 px-3 py-2 text-teal-200 hover:border-white hover:text-white">
        <svg class="h-3 w-3 fill-current" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path></svg>
      </button>
    </div>
    <div id="navMenuContainer" class="hidden w-full gap-4 sm:flex sm:w-auto sm:items-center">
      <div id="navMenuLinks" class="text-sm sm:flex-grow">
        <a href="#" class="mt-4 mr-4 block text-teal-200 hover:text-white sm:mt-0 sm:inline-block"> Blog</a>
        <a href="#" class="mt-4 mr-4 block text-teal-200 hover:text-white sm:mt-0 sm:inline-block"> About</a>
        <a href="#" class="mt-4 mr-4 block text-teal-200 hover:text-white sm:mt-0 sm:inline-block"> Gallery</a>
        <a href="#" class="mt-4 mr-4 block text-teal-200 hover:text-white sm:mt-0 sm:inline-block"> Events</a>
      </div>
      <div>
        <a href="#" class="mt-4 inline-block rounded border border-white px-4 py-2 text-sm leading-none text-white hover:border-transparent hover:bg-white hover:text-teal-500 sm:mt-0">Join Mailing List</a>
      </div>
    </div>
  </nav>
</header>

<script>
  const hamburger = document.getElementById("hamburger");
  const navMenuContainer = document.getElementById("navMenuContainer");
  hamburger?.addEventListener("click", () => {
    console.log(navMenuContainer);
    navMenuContainer?.classList.toggle("hidden");
    navMenuContainer?.classList.toggle("animate-pulse");
  });
  console.log(hamburger);
</script>
